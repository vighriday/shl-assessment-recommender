# Pinned for reproducible builds on the deploy host.
# Versions verified current and secure as of July 2026; bump deliberately, not
# implicitly. Requires Python >= 3.12 (numpy 2.x floor).

# API service
fastapi==0.138.2
uvicorn==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2

# Retrieval — lexical (TF-IDF) only. A sentence-embedding stage was trialled and
# removed for adding no measurable Recall@10 on the sample traces while pulling in a
# heavy, version-fragile dependency chain (torch/transformers/huggingface-hub). See
# docs/retrieval_design.md.
scikit-learn==1.9.0
numpy==2.5.0

# LLM access (provider-agnostic).
# Note: litellm 1.82.7/1.82.8 were briefly compromised in a March 2026 supply-chain
# incident; every release from 1.83.0 onward ships through the hardened pipeline.
# This pin is well clear of the affected versions.
litellm==1.90.2

# Chat UI, mounted onto the API for a human-facing way to test the agent. The HTTP
# API (/health, /chat) is unaffected; the UI is served alongside it at the root path.
gradio==6.19.0

# Testing
pytest==9.1.1
hypothesis==6.155.7
httpx==0.28.1
